const guest = document.querySelector("#guest-2")
console.log(guest)

// Pega só o primeiro
const guestByClass = document.querySelector(".guest")
console.log(guestByClass)

// Pega todos
const guestsByClass = document.querySelectorAll(".guest")
console.log(guestsByClass)
